# NPM Configuration
# Keep optional dependencies enabled (required for esbuild)
# The .npmrc will be ignored on Netlify if needed

# Use lockfile for consistent builds
package-lock=true

# Engine strict - ensure correct Node version
engine-strict=true
